using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClampTransform : MonoBehaviour
{
    public bool clampPosX = false, clampPosY = false, clampPosZ = false;
    public bool clampRotX = false, clampRotY = false, clampRotZ = false;

    // Update is called once per frame
    void Update ()
    {
    }
}
